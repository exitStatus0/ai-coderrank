import Dashboard from '@/components/Dashboard';

/**
 * Home Page
 * 
 * Server component that renders the client-side Dashboard.
 * Uses server component for initial render optimization.
 */
export default function HomePage() {
  return <Dashboard />;
}
